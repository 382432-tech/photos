# Project notes

This is a React 18 and TypeScript app built with Vite and Tailwind CSS.

## Run

- Development server: `npm run dev`
- Type check: `npm run lint`
- Production build: `npm run build`

The development server listens on `0.0.0.0:5000` so it works in Replit's web preview.